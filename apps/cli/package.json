{
  "name": "@trust-gate/cli",
  "version": "0.1.0",
  "description": "Trajectory-aware regression gate for autonomous coding agents -- CLI + stdio MCP server",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lzvxck/trust-gate.git",
    "directory": "apps/cli"
  },
  "homepage": "https://github.com/lzvxck/trust-gate#readme",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "regression-testing",
    "coding-agent",
    "ai-agent",
    "test-impact-analysis"
  ],
  "type": "module",
  "bin": {
    "trust-gate": "dist-cli/cli.js"
  },
  "files": [
    "dist-cli",
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "predev": "bun run --cwd ../../packages/core-graph build && bun run --cwd ../../packages/coverage-map build && bun run --cwd ../../packages/impact build && bun run --cwd ../../packages/test-runner build && bun run --cwd ../../packages/orchestrator build && bun run --cwd ../../packages/judge build",
    "dev": "xmcp dev",
    "prebuild": "bun run --cwd ../../packages/core-graph build && bun run --cwd ../../packages/coverage-map build && bun run --cwd ../../packages/impact build && bun run --cwd ../../packages/test-runner build && bun run --cwd ../../packages/orchestrator build && bun run --cwd ../../packages/judge build",
    "build": "xmcp build && bun run build:cli",
    "build:cli": "esbuild src/cli.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist-cli/cli.js --external:ts-morph",
    "postbuild": "node -e \"require('fs').writeFileSync('dist/package.json', JSON.stringify({ type: 'commonjs' }))\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "ts-morph": "^24.0.0"
  },
  "devDependencies": {
    "@trust-gate/judge": "workspace:*",
    "@trust-gate/orchestrator": "workspace:*",
    "esbuild": "^0.24.2",
    "openai": "^6.45.0",
    "typescript": "^5.7.3",
    "xmcp": "^0.6.0",
    "zod": "^4.0.0"
  }
}
