{
  "name": "@trust-gate/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/orchestrator/src/index.js",
  "types": "./dist/orchestrator/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests \"--path-ignore-patterns=**/__fixtures__/**\" \"--path-ignore-patterns=**/dist/**\""
  },
  "dependencies": {
    "@trust-gate/core-graph": "workspace:*",
    "@trust-gate/coverage-map": "workspace:*",
    "@trust-gate/impact": "workspace:*",
    "@trust-gate/test-runner": "workspace:*",
    "ts-morph": "^24.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.4",
    "typescript": "^5.7.3",
    "vitest": "3.2.4"
  }
}
